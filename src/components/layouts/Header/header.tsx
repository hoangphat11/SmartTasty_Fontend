"use client";

import Nav from "./NavBar";

export default function Header() {
  return (
    <header className="border-b-2 border-border text-text shadow-md bg-background transition-colors">
      <Nav />
    </header>
  );
}
